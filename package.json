{
  "name": "cims-hr-console",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "predeploy": "node scripts/apply_hotfix.mjs",
    "test": "node --test",
    "migrate": "wrangler d1 migrations apply cims-hr-console --remote",
    "migrate:staging": "wrangler d1 migrations apply cims-hr-console-staging --remote --env staging",
    "deploy": "npm test && npm run migrate && wrangler deploy",
    "deploy:staging": "npm test && wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
