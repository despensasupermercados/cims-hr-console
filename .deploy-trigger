deploy trigger 2026-07-08T — Fixed the card↔modal city mismatch (Miguel: card showed Orlando ON, modal showed Miami embark). Root cause: the card derives the city from the ship itinerary via resolveCity (Orlando/Port Canaveral on the sign-on date, green=derived), while the Edit-contract modal was showing the raw stored contract_edit.embark, which held a stale hand-typed "Miami" (ship_leg had the correct Orlando; only the overlay was stale — same class of staleness on 3 contract_edit rows). Decision (Miguel): itinerary wins. Fix: editContractModal now shows the resolved cities e.on_city / e.off_city (the exact values the card shows), read-only + muted, labelled "· from itinerary", falling back to stored embark/disembark only if no derived city exists. Board was already correct; this makes the modal agree. On save the resolved city is written back to embark, self-healing the stale overlays over time. 2 surgical single-line edits; deployed file BYTE-IDENTICAL to locally node --check'd target. NOTE: the 3 stale contract_edit city values remain in the DB but are now cosmetically irrelevant (ignored for display); flagged that sign-off emails, if they read raw embark, could still show a stale city — separate follow-up.
