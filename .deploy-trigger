deploy trigger 2026-07-08d — worker.js restored to good original (byte-perfect e60ad72) after an accidental full-file overwrite, then wired SURGICALLY (4 single-line in-place edits, node --check clean): Data → Upload data "Vessel deployment" now embeds the real loader (/api/relief/deploy) in an iframe instead of the old preview-only. Option relabeled "Celebrity / RCCL + Azamara". Old client-side preview guarded off. RULE: worker.js is edited surgically in place ONLY — never pushed as a whole file.
