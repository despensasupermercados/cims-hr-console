deploy trigger 2026-07-08N — Keyman ship rows now render the mockup two-card layout inline: the onboard crew card and, right beside it in the same grid, the reliever card (or a "+ Add reliever" empty slot tinted red/amber by urgency), with a full-width handover/urgency banner below (Clean handover · city · date / Reliever needed - N days / Reliever due / Slot open). The old thin "Relief ·" line above the card is gone, and the redundant /relief iframe section below the board is removed. Reliever rendering uses window.RELIEF (already piped in). "+ Add reliever" opens /relief where the full New-reliever modal lives. Applied as 5 surgical single-line edits in worker.js; the pushed file is BYTE-IDENTICAL to the locally node --check'd target (diff empty). NEXT (flagged to Miguel): port the New-reliever modal inline so add/edit happens without leaving the Keyman board.
