deploy trigger 2026-07-08M — Relief board (the section after the Keyman board) now matches the designed mockup. Modal: Match handover button (fills sign-on = printer OFF, sign-off = +min months, derives cities); "Crew member — required to activate"; plain Sign-on/Sign-off date pickers that live-derive the city from the picked date (green=turnaround, amber=nearest/non-turnaround); Confirmed toggles header; Sign-off workflow rows (Send instructions / sign-off link / review invite) with Mark-sent state persisted via /api/relief/save; Comment box + Post persisted to new relief_comment table. Board cards: empty "+ Add reliever" slot now tinted red/amber/neutral by urgency; Clean-handover banner shows city + date. Backend: fixed a latent bug where /api/relief/save rejected any {id,...} payload (id not whitelisted) — edit/mark-sent now work; added relief_comment table + GET/POST /api/relief/comment(s). NOTE: the three workflow buttons RECORD sent-state only (timestamp) — they do NOT fire an actual email to crew; real dispatch needs templates + Miguel's go. Pushed relief_ui.js + relief_api.js full-file via connector; node --check clean on outer module + inner browser script; tokens verified on main.
