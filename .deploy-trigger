deploy trigger 2026-07-08S — Keyman crew card redesigned to the approved mockup. Each card now: initials avatar (green when onboard) + name + PS chip + status/tenure stacked in a header; a clean "rotation" block below a hairline divider with labeled on/off rows — city on the left (confidence-colored), date right-aligned and non-wrapping; confirmed items as green tag pills (ECCR / AIR / HOTEL / ON DATE / OFF DATE); the "OFF in Nd" urgency chip stays top-right. Implemented by defining a redesigned rotCard AFTER the original (function-declaration override, last wins) so the change is a single clean insertion, plus one CSS block; inline handlers replaced with delegated rcClick/rcDrag (data-crew/data-seq) to avoid quote-escaping. Applied as 2 surgical single-line edits; deployed file BYTE-IDENTICAL to locally node --check'd target. Note: this rotCard is shared, so pool/shore crew cards get the same cleaner styling.
