deploy trigger 2026-07-08L — Keyman tab now shows the rich relief board BELOW the preserved Keyman/rotation board. Keyman board (rotationSections + all filters/chips) renders on top exactly as before; a "Relief board" section is appended after #rotbody as an iframe to /relief (the full reliever/printer mockup: urgency slots, handover bars, New reliever modal, Azamara off-override). Single verified find/replace edit in worker.js renderRotation; the iframe sits outside #rotbody so filter redraws (drawRotation) never touch it. node --check clean, 3849 lines, marker verified on main.
