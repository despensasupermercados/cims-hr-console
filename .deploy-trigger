deploy trigger 2026-07-08j — spec §3/§4.1 corrections on the Keyman Relief line: off-city and reliever join-city now render with CONFIDENCE color (derived=green, provisional=amber, seed=red, override=blue, TBA=grey) so the board no longer "lies"; handover status shown (clean / port mismatch / N-day gap) when a reliever exists; reliever name → join city + date added. Verified node --check + markers on main. Still open: P1 contract-card modal (old keyman card lets city be edited as truth — §5.1 invariant — needs rebuild/route to relief card); rotCard per-crew city confidence (needs on_conf/off_conf in the /api/rotation payload).