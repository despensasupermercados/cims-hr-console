deploy trigger 2026-07-07s — loader is now drag-&-drop and AUTO-RECOGNIZES the file by structure: Celebrity/RCCL wide "Export" sheet OR Azamara "Itinerary" sheet (NCL-ready). Azamara keyed off structure not names (names change; "Azamara " prefix stripped). Brand-aware reset (resetBrands) so each file cleanly replaces only its own ships — no stale rows. Self-verifies with per-brand counts + 12-month floor.
