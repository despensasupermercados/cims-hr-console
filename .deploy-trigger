deploy trigger 2026-07-08R — UX pass on the Keyman ship rows to match the mockup's printer card. (1) The onboard card + reliever slot now FILL the row width (.shipbody grid switched auto-fill→auto-fit, minmax 300px) instead of cramping into 240px columns with dead space on the right — this alone fixes most of the ugly wrapping. (2) Added the "OFF in Nd" urgency chip to the onboard card, top-right, colored by days-to-signoff (<=14 red, <=30 amber, else grey), computed from the crew's own sign-off date (self-contained, no relief dependency). (3) Sign-on / sign-off dates wrapped in white-space:nowrap spans so a date never breaks mid-token. Poolwrap grid left unchanged. Applied as 6 surgical single-line edits; deployed file BYTE-IDENTICAL to locally node --check'd target.
