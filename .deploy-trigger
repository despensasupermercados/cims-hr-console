deploy trigger 2026-07-08P — Clicking a reliever slot on the Keyman board now opens the New-reliever modal INLINE as an overlay (no navigation away). openRelief(el) reads data-vk off the clicked slot and mounts a full-screen overlay iframe to /relief?open=<vessel_key>. relief_ui.js embed mode: when ?open= is present it hides the board chrome, goes transparent, and auto-opens that ship's modal — so only the popup shows over the dimmed Keyman board behind it. On Save/Close the modal postMessages the parent; worker removes the overlay and, if something changed, calls renderRotation() to refresh the board in place. Both edits verified BYTE-IDENTICAL to locally node --check'd targets. relief_ui pushed via connector; worker.js via surgical single-line edit.
