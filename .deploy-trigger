deploy trigger 2026-07-07q — vpd-status now enforces a rolling 12-month forward-coverage floor per fleet ship and returns fleet_short_coverage (ships whose itinerary ends before today+12mo); loader displays both the no-deployment and short-coverage warnings after every upload.
