deploy trigger 2026-07-08O — UX pass on the Keyman relief pairing. Empty reliever slot redesigned: centered circled "+", "Add reliever", and an urgency chip ("OFF IN 144D"), with a subtle tinted background + dashed border colored by urgency (grey/amber/red), hover feedback, min-height matched to the crew card. Handover line is now an inline colored status pill with a leading status dot (Clean handover green / Reliever due amber / Reliever needed red / Slot open grey) instead of plain text. Filled reliever card cleaned up (navy RELIEVER chip). Applied as 2 surgical single-line overrides in worker.js (new fn + CSS declared after the old so they win); deployed file is BYTE-IDENTICAL to the locally node --check'd target (diff empty).
