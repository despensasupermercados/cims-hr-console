deploy trigger 2026-07-07r — loader page (/api/relief/deploy) now accepts the native RCCL/CEL wide "Export" .xlsx directly: SheetJS client-side decode (proven identical to the validated pipeline — 37,592 rows, 0 miss / 0 extra) → chunked POST to hardened /api/relief/vpd-load → auto-verify with 12-month coverage floor. Clean .tsv still accepted. No sandbox pre-processing needed.
