deploy trigger 2026-07-08a — printer (1st) card now resolves sign-on/sign-off to the deployment's turnaround ports (exact-date match), shown "CITY · date" like the reliever card, with "· turnaround" label when the date is a real turnaround. Exact-match (not snapped) so it never diverges from the reliever's follow-the-printer logic; honest fallback to derived/seed when out of coverage. Read-only preserved (dates owned by Keyman board).
