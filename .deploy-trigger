deploy trigger 2026-07-08b — printer (1st) card read-only UX cleanup: identity stays in the header; body is now a clean "Ship" line + a "Rotation" block (sign-on → sign-off, CITY · date, arrow between). Hides the editable-looking Ship dropdown, the redundant crew-search block, and the Confirmed toggles when read-only. Reliever/editable card unchanged. Data logic untouched — live DB (Rita's load) remains authoritative.
