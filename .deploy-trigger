deploy trigger 2026-07-08Q — Removed the flash between clicking "+ Add reliever" and the modal appearing. Now: (1) the overlay paints an instant dim backdrop the moment you click (immediate feedback); (2) the /relief iframe starts at opacity:0 so the relief page never shows while loading; (3) relief_ui, once it has hidden its board chrome and opened the modal, postMessages {t:'reliefReady'} to the parent, which fades the iframe in — so the very first thing visible is the modal itself, over the dimmed Keyman board. Also made the in-iframe modal backdrop transparent so the parent overlay provides the single, clean dim (no double-darkening). Both files verified BYTE-IDENTICAL to locally node --check'd targets; relief_ui + worker.js edits, tests gating.
