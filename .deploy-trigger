deploy trigger 2026-07-08c — printer (1st) card CONFIRMED toggles are back and ACTIVE: ECCR/AIR/HOTEL/ON DATE/OFF DATE are interactive + Save persists to the new leg_flags table (keyed by vessel_key + crew, so a rotation resets). Board reads leg_flags into printers; card + tag strip reflect saved state. Rotation (sign-on/sign-off) stays read-only — Keyman board still owns the dates. New endpoint POST /api/relief/leg-flags; new table leg_flags created on prod (additive).
