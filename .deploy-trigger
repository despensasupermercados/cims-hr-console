deploy trigger 2026-07-08i — ONE combined Keyman board. Keyman tab keeps everything (crew history, counts, filters, drag-reassign) and now folds in a per-ship "Relief" line: projected sign-off (Azamara-corrected turnaround), reliever (or "unassigned"), and urgency (colored border). renderRotation fetches /api/relief/board alongside /api/rotation, indexed by vessel_key (brand-bridge Royal→Royal Caribbean). Stray separate "Relief" nav tab removed. Surgical worker.js edits, node --check clean, 6/6 markers verified on main.
