deploy trigger 2026-07-08h — console now has a "Relief" nav tab (surgical worker.js edit: nav button + show() dispatch case + renderRelief() that embeds /relief in an iframe). Rita can open Relief in the console and review the live board — printers with Azamara 5-month projections, ready for relievers. node --check clean.
