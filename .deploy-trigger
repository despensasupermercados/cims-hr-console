deploy trigger 2026-07-08U — Per Miguel: the Edit-contract modal's Embark/Disembark are now port DROPDOWNS seeded from the ship's itinerary, not free text / read-only. New client helper portOptions(ports,date,current): filters vessel_port_day to turnaround ports (is_turnaround=1, not sea), windows to the port on the given date + the next ~5, and preselects the one matching the (Rita-verified) sign-on / sign-off date — "match the verified date to the right port name." Falls back to the current derived city if the date isn't an exact port day, and to "— no ports —" if the ship has no coverage. Modal fetches /api/relief/ports?ship=<e.ship> on open. saveContract stores the selected port_name. This removes free-typing (the source of the stale "Miami"). Reliever embark = onboard crew's sign-off day/port is the relief modal's Match-handover (already built). 4 surgical single-line edits (helper insert, ports fetch, 2 field swaps); deployed file BYTE-IDENTICAL to locally node --check'd target.
