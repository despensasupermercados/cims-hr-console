deploy trigger 2026-07-08g — Azamara off-override is now only written when Rita actually changes the sign-off dropdown (azTouched guard). Saving the printer card for any other reason (e.g. flipping a confirmation toggle) no longer clears an existing override. Reset per modal open. Reliever picker untouched.
