deploy trigger 2026-07-07p — hardened vessel-deployment ingestion: server validates every row (brand enum, YYYY-MM-DD date, non-empty ship/port), skips + counts malformed; GET /api/relief/vpd-status verify (counts, turnarounds, date range, fleet-ships-without-ports); loader auto-verifies + reports after load.
