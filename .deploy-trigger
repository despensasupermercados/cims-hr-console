deploy trigger 2026-07-08k — P2 done: the Keyman board now derives each crew leg's city from vessel_port_day via resolveCity and colors the ON/OFF city tokens by confidence (derived=green, provisional=amber, seed=red, override=blue, TBA=grey). rotationSections loads vessel_port_day once + groupPortDays; each crew object carries on_city/on_conf/off_city/off_conf; rotCard renders them colored, falling back to embark/disembark/TBA. Applied as 5 verified single-line edits, one at a time with per-edit verification (after two whole-file near-misses). node --check clean, 3849 lines, 6 markers verified on main. Next: P1 (override-only writable city + read-only derived modal + backfill).