deploy trigger 2026-07-08f — Azamara printer sign-off is now PROJECTED: next turnaround on/after sign-on + 5 months (~5–5.5mo), never before today. CEL/RCI unchanged (their stored offs already land on turnarounds). Printer card shows an adjustable Azamara sign-off dropdown (default = auto 5-month projection; pick any turnaround to override; save writes leg_flags.override_off_date; auto clears it). Partial-safe leg-flags upsert so override and confirmation toggles don't clobber each other. New col leg_flags.override_off_date (additive).
