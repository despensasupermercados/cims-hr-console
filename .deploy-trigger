deploy trigger 2026-07-08e — Azamara turnaround detection corrected per operator rule: a "Day" cell that is "1" or ends in "/1" (e.g. "12/1") is the crew-change turnaround; Day read as STRING (parseInt was dropping the "/1"). Old cruise-change guess was a day off. Now 261 turnarounds across the 4 Azamara ships (Journey → Stockholm/Copenhagen, correct). Re-drop the Azamara Itinerary file to refresh vessel_port_day.
